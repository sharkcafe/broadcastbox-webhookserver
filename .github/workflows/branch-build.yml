name: branch-build
on:
  push:
    branches-ignore:
      - main
    tags-ignore:
      - '**'
  pull_request:

permissions:
  contents: read

jobs:
  branch-build:
    name: branch-build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Run Go tests
        run: go test -v ./...
